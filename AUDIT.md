# GIGA Standard v5 監査：ひらがな・カタカナ かきかたマスター

- 対象：`GIGAyama/KANA_Master`（**A型**：単一 HTML ＋ React。ビルドを 導入した）
- 監査日：2026-08-04
- 基準：GIGA Standard v5
- 計測環境：Chromium 141（実ブラウザ）／1366×768 と 320×568 の 2 解像度 ×
  8 画面（ホーム・かく（もじ表）・かく（れんしゅう）・よむ・とくべつな おと・
  ことばずかん・はんこずかん・リセットの かくにん）

> **この作業環境は `cdn.jsdelivr.net` / `unpkg.com` / `cdn.tailwindcss.com` /
> `fonts.googleapis.com` へ 出られない。**
> つまり **学校の フィルタリングと まったく 同じ状態**で 測れている。
> `gigayama.github.io` と `script.google.com` へは 到達できないため、
> **本番での 確認は していない**（下の「測っていないもの」を 見ること）。

---

## いちばん大きな問題（P0.5）

**改修前は、学校のネットワークで CDN が ふさがれていると 画面が 何も 出なかった。**

`index.html` が ブラウザへ 送っていたもの：

| もの | 大きさ | ふさがれると |
|---|---:|---|
| `cdn.tailwindcss.com` | — | 色・レイアウトが 一切 当たらない |
| `unpkg.com/react@18` | — | **起動しない** |
| `unpkg.com/react-dom@18` | — | **起動しない** |
| `unpkg.com/@babel/standalone` | 約 3MB | **起動しない**（うえに ひらくたび 42万字を コンパイル） |
| `cdn.jsdelivr.net`（KanjiVG） | 使うたび | **お手本も かきじゅんアニメも なぞり書きも 出ない** |

最後の 1 行が このアプリでは とくに 重い。
**「かきかたマスター」の 中心機能そのもの**が、アプリの外の 事情で 止まる。
児童からは「こわれている」としか 見えず、原因は アプリの外に あるので
先生が しらべても わからない。

### 実測（§7-6 のとおり、改修前の commit を 実際に 出して 測った）

改修前（`8ebed1d`）を そのまま 手元のサーバーに 置き、
改修後と **同じ ブラウザ・同じ ネットワーク条件**で ひらいた。

| | 改修前 `8ebed1d` | 改修後 |
|---|---|---|
| `#root` の 文字数 | **8**（「よみこみちゅう…」のまま） | **486** |
| 押せる ボタン | **0 個** | **23 個** |
| 読みこみ 失敗ホスト | unpkg.com / cdn.tailwindcss.com / fonts.googleapis.com | fonts.googleapis.com のみ |
| JS エラー | 7 件 | 0 件 |

fonts.googleapis.com だけは 改修後も 失敗するが、これは **見た目だけ**の
依存で、字の形が かわるだけで アプリは そのまま 動く（§2-7 のとおり
端末側フォントを 後ろに 並べてある）。

### 直しかた

すべて **先に つくっておく** 形に した。

| もの | 前 | 後 |
|---|---|---|
| Tailwind | ブラウザ内で CSS を 生成 | ビルド時に 使うクラスぶんだけ 生成（`css/app.css`） |
| JSX | `@babel/standalone`（毎回 コンパイル） | **ビルド時に 1回だけ**（`js/app.js`） |
| React / ReactDOM | unpkg | npm で 版を固定して 自分側へ（`vendor/react.js`） |
| かきじゅんデータ | 使うたび jsDelivr | **リポジトリの中**（`data/kanjivg-kana.js`・176文字・49KB） |

**CDN から 取る 実行コード：0 バイト。**

---

## 監査表

判定の 意味 — ✅ 満たしている ／ ❌ 満たしていない ／ ⚠️ 条件つき ／ **未計測**

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT（新規作成）。KanjiVG は 別条件のため 追記あり |
| A2 | .gitignore | ✅ | `node_modules/` `.env` `.DS_Store` ほか。生成物は **わざと 追跡する**（GitHub Pages が リポジトリを そのまま 配るため） |
| A3 | dependabot.yml | ✅ | 新規作成。monthly。**メジャー更新は 無視**（React 18→19 は 人が 見てから） |
| A4 | README / MANUAL / AUDIT | ✅ | MANUAL.md・AUDIT.md を 新規作成。README に ビルド節・原本/生成物の表を 追記 |
| A5 | 第三者ライセンスの明記 | ✅ | `THIRD-PARTY-NOTICES.md` を 新規作成。**KanjiVG は CC BY-SA 3.0（継承）** |

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで **動作確認済み**） | ✅ | 新規投入。**8画面 × 2解像度を 歩いて CSP違反 0件・JSエラー 0件** |
| B2 | 秘密情報・IDの直書きなし | ✅ | `.clasp.json` `.env` の 追跡なし。API キー・シート ID なし（このアプリは サーバーを 持たない） |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を 使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` は Service Worker への `{type:'SKIP_WAITING'}` のみ |
| B5 | サーバー側5段ガード | — | 該当なし（記録は 端末内だけ・送信しない） |
| B6 | **CDN から取る実行コードが 0** | ✅ | **0 バイト**（上の表） |
| B7 | 残る外部資産に SRI と版の固定 | ⚠️ | 残る外部資産は Google Fonts の CSS 1本のみ。**Google Fonts は 中身が 端末ごとに 変わる 設計なので SRI を 付けられない。**「止まっても 動作に 影響しない」ものなので §2-7 の 判断に 従い CDN のままとした |

CSP の 中身（`index.html`）：

```
default-src 'self'; script-src 'self';
style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
font-src 'self' https://fonts.gstatic.com;
img-src 'self' data: blob:; connect-src 'self';
worker-src 'self'; manifest-src 'self';
object-src 'none'; base-uri 'self'; form-action 'self';
```

- `script-src 'self'` を 通すため、**インラインの `<script>` 3本を 外部ファイルへ 切り出した**
  （`js/install-hook.js` / `js/boot.js`、および 本体は `js/app.js`）。
  `onclick=` は もともと 使われていなかった。
- `style-src` の `'unsafe-inline'` は React の `style={{…}}` のため。外せない。
- `connect-src 'self'` に できたのは、**かきじゅんデータを アプリの中に 持ったから**。
- **`frame-ancestors` は 書いていない。** `<meta>` では 無視され、
  読みこむたび コンソールに 警告が 出るだけになる。GitHub Pages では
  HTTP ヘッダーを 足せないため、`index.html` の コメントに 理由を 残した。

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし（GAS なし） |
| C2 | 自動復旧 | — | 該当なし |
| C3 | pagehide で記録確定 | ✅ | `studySession.js` が `pagehide` で 確定（Chromebook の タブ破棄対策）。既存 |
| C4 | 通信失敗時のリトライと明示 | ✅ | **通信そのものが なくなった**（かきじゅんデータを 内蔵）ので 失敗しようが ない |
| C5 | localStorage.clear() を使っていない | ✅ | 0 件。リセットは 自アプリ接頭辞のキーだけ（既存） |

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | あり |
| D2 | 100dvh を使用 | ✅ | `100vh` の 単独使用 0 件（`@supports` フォールバック付き） |
| D3 | safe-area-inset を適用 | ✅ | `.kkm-app-root` の 上下左右 ＋ 更新トーストの 下端 |
| D4 | clamp() による fluid type | ⚠️ | `offline.html` は clamp。本編は Tailwind の 段階指定＋高さ別メディアクエリで 組んである。**基準は 満たしていないが、実測で 320px〜1366px の 全画面で 破綻 0 件**だったため 触っていない（§P1 の 目的は 破綻の 解消） |
| D5 | Canvas に devicePixelRatio 補正（上限2） | ✅ | `Math.min(window.devicePixelRatio \|\| 1, 2)`。既存 |
| D6 | **320px 幅で 横スクロールが出ない** | ✅ | **8画面すべて 0px** |
| D7 | 画像に width/height、150KB以下 | ✅ | 最大 `mascot-full.png` 38.9KB。`<img>` 2 か所は 装飾（`alt=""`）で サイズ指定あり |
| D8 | **コントラスト 4.5:1 以上（実ブラウザで全画面走査）** | ✅ | **100件超 → 0件**（下に 内訳） |
| D9 | **タップ領域 44px 以上（::after 込みで実測）** | ✅ | **23種類/のべ98件 → 0件** |
| D10 | prefers-reduced-motion（`.01ms` であって 0 でない） | ✅ | `0.001ms`。0 ではないので `fill-mode: forwards` は 生きている |
| D11 | forced-colors 対応 | ✅ | 新規追加（ボタンに `border: 2px solid ButtonText`） |
| D12 | 提示モード | ❌ | **未実装。** 機能追加になるため Part III の 規則に 従い 提案に とどめた（下の「人間に決めてほしいこと」） |
| D13 | 印刷CSS | ⚠️ | 印刷して 配る画面は ない。それでも Ctrl+P で 固定要素（更新トースト・紙吹雪）が 紙に 出ないよう 最低限を 追加。**印刷プレビューでの 目視確認は していない** |
| D14 | **拡大を禁止していない** | ✅ | `maximum-scale=1.0, user-scalable=no` を **削除**。誤ズームは `touch-action: manipulation` で 抑える |

#### D8 コントラストの内訳（前 → 後）

一括置換では なく、**濃い面の上の文字が ないことを 先に 確かめてから**
文字色だけを 1〜2段 濃くした（面・わくの `bg-` / `border-` は 触っていない）。

| 出どころ | 色 | 紙(#faf7f0)の上 | 置き換え先 | 後 | 件数 |
|---|---|---:|---|---:|---:|
| `text-sumi-300` | `#c3bcae` | **1.76** | `text-sumi-600` | 6.87 | 8 |
| `text-sumi-400` | `#9b9386` | **2.84** | `text-sumi-600` | 6.87 | 18 |
| `text-sumi-500` | `#7a7267` | **4.43** | `text-sumi-600` | 6.87 | 45 |
| `text-yamabuki-600` | `#9c6e1d` | **4.21** | `text-yamabuki-700` | 6.19 | 4 |
| `text-shu-400` | `#d97e65` | — | `text-shu-700` | — | 2 |

とくに 読めなくて 困っていたもの（§2-8 の「案内・空状態が いちばん 読みにくい」）：

| 文言 | どういう文か | 前 | 後 |
|---|---|---:|---:|
| 「もじを えらんでください」 | 空状態の 案内 | **1.89** | 7.35 |
| 「なにを かこうかな？」 | ことば入力の プレースホルダ | **1.89** | 7.35 |
| `?`（こたえる マス） | **ここに 答える、という 合図** | **1.89** | 7.35 |
| 「二／なぞる」「三／じぶんで」 | 次の 段階の 名まえ | **1.76** | 6.87 |
| 「あ行」〜「わ行」 | もじ表の 行見出し | **2.84** | 6.87 |

さらに、色は 十分でも `opacity` で うすくしている 5 か所を 見つけた。
**`opacity-70` を 掛けると、`text-sumi-700`（9.75）でも 実際の 見え方は 3.39 まで 落ちる。**
段階の ちがいは 色と わくで 伝わるので、文字の `opacity` は 外した。

#### D9 タップ領域の内訳

`.kkm-btn` は アプリの 全ボタン（81 か所）に 付いていたので、
**1つずつ `.tap-44` を 足して 回らず、まとめて 当たり判定を ひろげた。**
1か所ずつ 潰すと かならず 付け忘れが 出る。

- `.kkm-btn::before` で 見た目を 変えずに 当たり判定だけ 44×44 以上に
  （`::after` は 朱色の波紋 `.kkm-ripple` が 使っているので `::before`）
- `:where(.kkm-btn) { position: relative }` と **詳細度 0 で 書いた。**
  ふつうに書くと、すでに Tailwind の `.absolute` が 付いている
  「とじる」ボタンの 位置指定を 上書きして こわす
- フッターの 「GIGA山」リンクは **42×14px** しか なかった → `tap-44` を 付与
- はやさスライダーは **168×8px**。`input` は 疑似要素を 持てないので、
  帯の 見た目は 8px の まま `input` の 高さだけ 44px にし、
  つまみを 24px の 丸に した
  （※ 古い `input[type=range] { height: 8px }` が あとから 上書きして
  効いていなかった。あとの方の 規則を 消して 解決）

### E. PWA（Part I §3）

**すべて 実ブラウザで 動かして 測った。9/9 合格。**

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url が リポジトリ名絶対パス | ✅ | `/KANA_Master/`（既存で 正しかった） |
| E2 | アイコン4種 + **透明を含まない** apple-touch-icon | ✅ | `apple-touch-icon.png` 180×180・**アルファチャンネルなし**（画素を 数えて 確認） |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | `js/install-hook.js`。`<head>` の 最初の `<script>` |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | 既存。`display-mode: standalone` で 判定 |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除（**実測**） | ✅ | 他アプリ名の キャッシュを 2本 置いて 版を 上げ、**2/2 のこった** |
| E6 | sw.js が localStorage に触れていない | ✅ | 0 件 |
| E7 | 更新通知（押すまで切りかわらないことを実測） | ✅ | 版を 上げて **3秒 放置 → `waiting` のまま**。押すと 切りかわり 1回だけ 読みこみ直す |
| E8 | **初回訪問で勝手にリロードしない** | ✅ | まっさらな 状態で **画面遷移 1 回** |
| E9 | **Service Worker が実際に登録されている** | ✅ | `getRegistration()` → `scope=…/KANA_Master/`、`active` あり |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | **インラインの `<script>` を 削除し `<a href="./">` に した**（CSP でも 止まらない） |
| E11 | APP_VERSION を今回のリリース値に更新した | ✅ | `v13` → `v14` |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ⚠️ | **画素を 数えた結果 1.30%。** ただし その 1.30% は **すべて 下辺へ 抜ける えんぴつの 軸**で、欠けては 困る 中身（「あ」「ア」と えんぴつの 先）は **0.00%**。円マスクを 実際に 当てた 見え方も 確認した |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | `MANUAL.md` §1 に 端末別の 表 |

改修前の `sw.js` にあった 問題（すべて 直した）：

| 何が 起きていたか | 直しかた |
|---|---|
| `install` で `skipWaiting()` していた | 外した。画面側で 押されるまで 切りかえない |
| 更新を 見つけると **黙って** `SKIP_WAITING` を 送っていた（利用者に 聞かない） | 「あたらしい ばんが あります／さいしんに する」を 出すように した |
| Service Worker の 登録が `window.addEventListener('load', …)` だけ | `document.readyState === 'complete'` の 分岐を 追加 |
| CDN 4本を 先読みキャッシュに 入れていた | CDN が 無くなったので 一覧ごと 削除 |
| `controllerchange` の 扱いが 無かった | 「**利用者が 押したときだけ**」受ける 形に した |

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | `<img>` 2 か所は 装飾のため `alt="" aria-hidden="true"`。更新トーストに `role="status" aria-live="polite"` |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | 既存（`role="dialog" aria-modal="true"`）。実測で モーダル 2 画面を 歩き JS エラー 0 |
| F3 | キーボードのみで全機能に到達 | **未計測** | Tab 順の 目視確認を していない |
| F4 | rt の色を決め打ちしていない | ✅ | このアプリは ルビ（`<ruby>`/`<rt>`）を 使っていない。検査は 入れた |
| F5 | 初回JS 300KB以下 | ❌ | **509KB**（gzip 145KB）。改修前の 3.3MB超 からは 大きく 減ったが 基準未達（下） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ❌ | `src/App.jsx` **7,471行 / 417KB**（下） |

#### F5 性能の実測

| ファイル | 素 | gzip |
|---|---:|---:|
| `js/app.js` | 272.7 KB | 65.7 KB |
| `vendor/react.js` | 139.5 KB | 46.0 KB |
| `data/kanjivg-kana.js` | 50.0 KB | 15.5 KB |
| `studySession.js` | 30.8 KB | 10.3 KB |
| `js/boot.js` ＋ `install-hook.js` ＋ `studyLog.js` | 16.1 KB | 7.1 KB |
| **初回に必要な JS 合計** | **509.1 KB** | **144.7 KB** |
| 総アセット（初回・画像こみ） | 689.5 KB | — |

- 基準（素で 300KB 以下）には **届いていない**。ただし
  改修前は これに 加えて `@babel/standalone` 約3MB が 乗り、
  **ひらくたび 42万字を コンパイル**していた。
- 総アセット 689.5KB は 基準（1MB 以下）を 満たす。
- **LCP と CLS は 未計測**（本番に 到達できないため）。

#### F6 巨大ファイル

`src/App.jsx` が 7,471行 / 417KB。**分割していない。**
Part III P3 に「巨大ファイルの 分割は 自動で やらない。分割案を 提示 →
合意 → 1機能ずつ PR」と あるため、提案に とどめた（下）。
品質ゲートでは **警告あつかい**にして 数字を 出し続ける
（❌ で CI を 止め続けても 誰も 直せないため）。

### G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | `node tools/check-study.js` **154件 すべて 合格** |
| G2 | 中断記録・5分ルール | ✅ | 同上（既存の テストに 含まれる） |

---

## 品質ゲート

`node tools/check-project.mjs`（CI と 同じもの）。20 項目。

**わざと 壊して 通ることを 確かめた**（`--self-test`）。
「0件でした」だけでは、検査が 動いているのか 何も 見ていないのか
区別が つかない。実際、この 確認で **自分が 書いた 検査の 不具合が 2件** 見つかった。

| 見つかった不具合 | 中身 |
|---|---|
| `CSP` の 取りこぼし | `content=["']…["']` と ゆるく 受けたため `default-src 'self'` の シングルクォートで 切れ、**いつも「object-src が ない」に なっていた** |
| `NO_INLINE_SCRIPT` の 誤検知 | 「CSP を 入れると `<script>` が 動かなくなる」と 書いた **説明文**（HTMLコメント）に 反応。判定前に コメントを 落とす |

`BUILD_IS_FRESH`（原本を 直したのに `npm run build` を 走らせ忘れた、を 見つける）も
実際に 原本だけ 直して 落ちることを 確認した（`AM js/app.js`）。

---

## 実測に つかった道具（このリポジトリに 入れた）

| ファイル | 何を するか |
|---|---|
| `tools/serve.mjs` | 本番と 同じ サブパス（`/KANA_Master/`）で 配る。`file://` では SW も CSP も manifest も 効かないため |
| `tools/measure-display.mjs` | コントラスト・タップ44px・320px の 横スクロール・CSP違反・JSエラー |
| `tools/measure-pwa.mjs` | SW 登録・初回リロード・更新の 待ち・他アプリの キャッシュ・圏外・offline.html |
| `tools/check-project.mjs` | 静的な 品質ゲート（`--self-test` 付き） |

### 測るときに ハマったところ（他のリポジトリでも 効く）

1. **SVG の `<text>` は `color` ではなく `fill` で 描かれる。**
   `color` を 読むと 継承した 別の色を 測ってしまい、**比 1.0 の 誤報**が 出る。
2. **`opacity` を 見ないと 実際より 良い数字が 出る。**
   `getComputedStyle(el).color` は `opacity` を 含まない。
   祖先の `opacity` を かけ合わせて 前景の α に 織りこむ。
   これを 入れて はじめて、上の「`opacity-70` で 3.39 に 落ちる」5件が 見えた。
3. **祖先が `opacity: 0` の 要素（アニメの 途中で あとから 出るもの）は 除く。**
   除かないと やはり 比 1.0 の 誤報に なる。
4. 色は 数字を 拾わず **1px 実際に 塗って `getImageData` で 読む**（§7-2）。

---

## 測っていないもの

**「未計測」は ✅ ではない。** つぎは 測っていない。

- **本番（`gigayama.github.io/KANA_Master/`）での 動作。**
  作業環境から 到達できない（プロキシが 通さない）。
  マージして GitHub Pages に 出たあと、**実機で 一度 ひらいて 確かめてほしい。**
  とくに 見てほしいのは つぎの 3 つ。
  1. 「アプリにする」が 出るか（`beforeinstallprompt`）
  2. すでに インストール済みの 端末で、**別アプリ扱いに ならないか**
  3. 更新の おしらせが 出て、押すと 切りかわるか
- **実機での 表示**（Chromebook・iPad の 実物）。計測は すべて デスクトップ
  Chromium の エミュレーションで 行った。
- **LCP / CLS**（本番に 到達できないため）。
- **キーボードのみでの 到達性**（F3）。Tab 順の 目視確認を していない。
- **印刷プレビューの 目視**（D13）。CSS は 入れたが 紙は 見ていない。
- **iOS Safari 固有の 挙動**（ITP の 7日ルール・`apple-touch-icon` の 実際の 見え方）。
  アイコンに 透明が ないことは 画素で 確認したが、**実機では 見ていない。**
- **読みあげ（`speechSynthesis`）** が CSP の もとで 動くこと。
  CSP の 対象外の API なので 影響は ない はずだが、実機で 音は 出していない。
- 自動採点の 精度。**かきじゅんデータの 出どころを CDN から 内蔵に 変えたが、
  中身は KanjiVG の 同じ SVG から 同じ `d` 属性を 取り出したもの**なので
  採点結果は 変わらない はず。ただし **全176文字を 突き合わせては いない**
  （「あ」の 3画・かきじゅんアニメの 表示は 実際に 見て 確認した）。

---

## 人間に 決めてほしいこと

1. **`src/App.jsx`（7,471行 / 417KB）を 分割するか。**
   分割案：`WORD_BANK`（713語）と `MIM` の 問題データを 別ファイルへ 出すだけで
   200KB 近く 減り、初回 JS も 基準に 近づく。
   ただし 分割後は **ESLint の `no-undef` を 必ず 通すこと**
   （import 漏れは ビルドを 通過して 実行時に 落ちる）。
   → **合意が あれば 別 PR で、1機能ずつ 進める。**
2. **提示モード（D12）を 足すか。**
   一斉授業で 電子黒板に 映して つかう 場面が あるなら 必要。
   機能追加に あたるため 今回は 触っていない。
3. **`studyLog.js` の 正本を どのリポジトリに 置くか**（v5 Part V の 未決事項）。
   このリポジトリの ものは 触っていない。
4. **生成物（`js/` `css/` `vendor/` `data/`）を コミットする 方式で よいか。**
   GitHub Pages が リポジトリを そのまま 配るため こう した。
   CI で ビルドして 成果物だけ 配る 方式にも できるが、
   その場合 リポジトリを クローンして ひらくだけでは 動かなくなる。
